# macOS
.DS_Store

# Local venv for icon compositing script
.venv-icon/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets and machine-local config (never commit real keys)
.env
.env.*
!.env.example
*.pem
*.p12
*.mobileprovision

# Node (local SVGO / tooling)
node_modules/

# Xcode (local DerivedData when using -derivedDataPath under Vectify/)
Vectify/.derivedData/
Vectify/.derivedData-release/

# Xcode user-specific (keep machine-local schemes/settings out of the repo)
**/xcuserdata/
*.xcuserstate

# DMG / archive staging (scripts/build-dmg.sh)
build/

# Local exported .app folders (e.g. drag-out from Archive); ship zip via GitHub Releases only
Vectify1.0(1)/
